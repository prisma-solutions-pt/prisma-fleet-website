/* Background effects are now embedded in the Hero via Waves component.
   This component is no longer needed but kept as a no-op for import compat. */
export default function Background() {
  return null;
}
